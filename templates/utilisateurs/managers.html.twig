

{% block title %}Hello UtilisateursController!{% endblock %}

{% block body %}
    <div>



        <div>
            {% for d in data %}
                <h2>{{ d.id }}</h2>
                <h2>{{ d.email }}</h2>
            {% endfor %}
        </div>
    </div>
{% endblock %}
