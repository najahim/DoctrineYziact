{% extends 'base.html.twig' %}

{% block title %}Hello OrganisationController!{% endblock %}

{% block body %}
    <style>

    </style>
    <div class="example-wrapper">
        {% for d in data %}
            <h1>{{ d.id }}</h1>
            <h1>
                {{ d.nomOrganisation }}
            </h1>
            <h1>
                {{ d.manager.email }}
            </h1>
            <h1>
                {{ d.type.typeOrganisation }}
            </h1>
        {% endfor %}
    </div>

{% endblock %}

