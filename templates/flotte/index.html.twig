{% extends 'base.html.twig' %}

{% block title %}Hello FlotteController!{% endblock %}

{% block body %}
<style>

</style>

<div class="example-wrapper">
    {% for d in data %}
        <h1>{{ d.id }}</h1>
        <h1>
                {{ d.manager.email }}
        </h1>
    {% endfor %}
</div>
{% endblock %}
